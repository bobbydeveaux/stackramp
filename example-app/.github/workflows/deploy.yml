name: Deploy

on:
  push:
    branches: [main]
  pull_request:

# This is the ONLY file a developer needs to add.
# Everything else is handled by the Launchpad platform.

jobs:
  deploy:
    permissions:
      id-token: write       # required for WIF / GCP auth
      contents: read        # required to checkout code
      pull-requests: write  # required to post PR preview URLs
    uses: bobbydeveaux/launchpad/.github/workflows/platform.yml@main
    secrets: inherit
